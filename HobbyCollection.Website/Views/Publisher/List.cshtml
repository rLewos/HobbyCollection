@model HobbyCollection.Website.ViewModels.PublisherViewModel

@if (Model.PublisherList != null && Model.PublisherList.Count > 0)
{
	<table id="tablePublisher" class="table table-light">

		<thead>
			<tr>
				<th>Name</th>
				<th> - </th>
			</tr>
		</thead>

		<tbody>
			@foreach (var item in Model.PublisherList)
			{
				<tr itemid="@item.Id">
					<td>@item.Name</td>
					<td>
						<button type="button"><i>I</i> Edit</button>
						<button type="button"><i>I</i> Remove</button>
					</td>
				</tr>
			}
		</tbody>

	</table>
}
else
{
	<p>There aren't any publishers.</p>
}


@section Scripts {
	<script type="text/javascript">
		$(() => {
			$("#tablePublisher");
		});
	</script>
}