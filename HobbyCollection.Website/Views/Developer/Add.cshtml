@model HobbyCollection.Website.ViewModels.DeveloperViewModel

<form asp-action="Save" asp-controller="Developer" method="post" id="formPlataform" class="row g-3">

	<input asp-for="Developer.Id" type="hidden" />

	<div class="col-md-6">
		<label asp-for="Developer.Name" class="form-label">Name: </label>
		<input asp-for="Developer.Name" class="form-control" value="" />
	</div>

	<div class="col-md-12">
		<button type="submit" class="btn btn-primary">Save</button>
		<button type="reset" class="btn btn-warning">Reset</button>
	</div>

</form>

@section Scripts {
	<script type="text/javascript">
		$(() => {

			$("#formDeveloper").on("submit", (e) => {
				e.preventDefault();

				const form = $("#formDeveloper").serialize();
				$.ajax({
					url: "/Developer/Save",
					type: "POST",
					data: form,
					success: (data) => {
						console.log(data);
					},
					error: (error) => {
						console.error(error);
					},
					complete: () => {

					}
				})
			});

		})
	</script>
}